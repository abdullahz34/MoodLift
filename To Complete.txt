calendar:

    when loading times grey out any booked time by fetching that day, then change the button to 2 buttons update/ delete

    if appointment is grey "update" button should be cancel for ambassador (delete route)

    remove sat sun from calendar

    add background to time and button

page:

    create a bookings component for usage by both employee and ambassador

    employee perspective and request ambassador

styling:

    scroll bar on bookings component


After merging:

authorisation (code from dashboard)









